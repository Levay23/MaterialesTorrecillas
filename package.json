{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"if(!/pnpm/.test(process.env.npm_config_user_agent)) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "dev": "concurrently \"cross-env PORT=3000 pnpm --filter @workspace/api-server dev\" \"cross-env PORT=5173 BASE_PATH=/ pnpm --filter @workspace/ferremax dev\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@firebase/util",
      "@whiskeysockets/baileys",
      "bcrypt",
      "protobufjs"
    ]
  }
}
